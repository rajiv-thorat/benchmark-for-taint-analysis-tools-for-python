FROM mcr.microsoft.com/cstsectools/codeql-container

ARG USERNAME=root

USER ${USERNAME}

RUN apt-get update && \
    apt-get -y install time

ENTRYPOINT ["time", "-o", "/opt/results/non_exec_metrics.txt", "python3", "/usr/local/startup_scripts/startup.py"]